#ifndef OUTPUT_ARRAY_H
#define OUTPUT_ARRAY_H

//Includes the Array structure definition from the statistics library.
#include "statistics.h"

/*Prints all values in an Array to the terminal.
The function does not return a value, so its return type is void const, basically no change to it*/
void output_array(const Array *array);

#endif

